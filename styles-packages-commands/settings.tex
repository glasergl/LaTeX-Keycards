% counter variable to control whether the digital version or the printable version should be produced
\newcounter{printVersion}
\setcounter{printVersion}{0} % set to 1 for the print version, e.g., rotates the titles for folding and removes the table of contents. For the digital version set to 0.

% setting this to > 0 may screw up the layout
\setlength{\parindent}{0cm}

% make itemize and enumerate more concise
\setlist[enumerate]{topsep=0pt,itemsep=-1ex}
\setlist[itemize]{topsep=0pt,itemsep=-1ex}

% add some metadata to the key card document (not visible for the print version)
\title{Subject}
\author{Max Mustermann}
\date{\today}

% uncomment if only using ksection, but not ksubsection etc.
% \KOMAoptions{toc=sectionentrywithdots}
% \setkomafont{sectionentry}{\normalfont}
